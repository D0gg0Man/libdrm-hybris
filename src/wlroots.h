/*
 * wlroots.h -- wlroots/phoc-specific behaviour.
 *
 * The libseat replacement is exported directly(phoc links libseat.so.1 and
 * this shim stands in for it), so there is nothing to declare for it here.
 * This header exists for the wlroots present path that libdrm-hybris.c calls
 * into.
 */

#ifndef LIBDRM_HYBRIS_WLROOTS_H
#define LIBDRM_HYBRIS_WLROOTS_H

#include "common.h"

#endif /* LIBDRM_HYBRIS_WLROOTS_H */
